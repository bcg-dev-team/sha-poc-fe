
  # SHA-make-desktop

  This is a code bundle for SHA-make-desktop. The original project is available at https://www.figma.com/design/wJzdPeyzVDuW6zTlNYV8oc/SHA-make-desktop.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  