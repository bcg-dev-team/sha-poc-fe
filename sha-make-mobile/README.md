
  # SHA-make

  This is a code bundle for SHA-make. The original project is available at https://www.figma.com/design/0DsCh2HurR9UvrMEgpJk6N/SHA-make.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  